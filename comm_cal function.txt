CREATE OR REPLACE FUNCTION COMM_CAL(PEMPNO IN NUMBER) RETURN NUMBER IS
MSAL EMP.SAL%TYPE;
MCOMM EMP.SAL%TYPE;
BEGIN
SELECT SAL,COMM INTO MSAL,MCOMM FROM EMP1 WHERE EMPNO=PEMPNO;
IF MSAL<2000 THEN 
MCOMM:=MSAL*10/100;
ELSE
MCOMM:=MSAL*15/100;
END IF;
RETURN MCOMM;
END;
